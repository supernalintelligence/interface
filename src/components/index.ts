/**
 * Component Name Contracts
 * 
 * Scanning and generation of type-safe component name contracts
 * based on data-testid attributes in React components.
 * 
 * NOTE: ComponentScanner is server-only (uses Node.js fs/glob modules)
 * It is NOT exported to prevent browser bundle issues.
 * Import directly if needed: import { ComponentScanner } from '@supernal/interface/src/components/ComponentScanner'
 */

// ComponentScanner is server-only - not exported to prevent browser bundle issues
// Use webpack configuration to stub it in browser builds

